import { expect } from 'chai';
import RedisIntegration from '../Integrations/Redis';

describe('Redis Integration', () => {
  it('should add new key-value pair', () => {});
  it('should throw error when adding same key', () => {});
  it('should add new key-value pair and retrieve it', () => {});
});
